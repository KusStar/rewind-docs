{
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "concurrently": "^7.2.1",
    "esbuild": "^0.15.13",
    "next": "^13.0.3",
    "turbo": "^1.6.3",
    "typescript": "^4.8.4"
  }
}
