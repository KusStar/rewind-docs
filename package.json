{
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "devDependencies": {
    "next": "^13.1.2",
    "turbo": "^1.7.0",
    "typescript": "^4.9.4"
  }
}
