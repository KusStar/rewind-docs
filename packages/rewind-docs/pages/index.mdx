---
overwrittenTitle: '倒带 App'
---

import Button from "../components/Button";
import { Landing } from "nextra-theme-rewind";


<Landing
  title="倒带"
  subtitle="简单的音乐播放器"
  keywords={['轻量', '简洁']}
  buttons={[
    {
      content: '下载',
      onClick: (router) => router.push('/download')
    },
    {
      content: '更多',
      type: 'ghost',
      onClick: (router) => router.push('/home')
    }
  ]}
/>
